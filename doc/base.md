### 变量/常量声明

- var x: int = 12
- x := 12
- const x = 1 // 字面量


### 数据类型

#### 基础数据类型
- 整数 
- 浮点数
- 布尔
- 复数

#### 复合数据类型
- 数据
  ```go
  	x := [2]int{1, 2}
    x := [...]int{1, 2}
  ```

- 切片 
  ```go
  	x := []int{1, 2}
    y := []int{1, 3}
    // ==、!= 仅仅 nil 其余不可以比较 
    // slices.Equal(x, y)
     
    // 
  ```
